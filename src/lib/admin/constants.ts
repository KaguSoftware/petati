/** Cookie remembering which store the admin is working on (owner store switcher). */
export const ADMIN_STORE_COOKIE = "admin_store";

/** Rows per page in admin lists. */
export const ADMIN_PAGE_SIZE = 25;
